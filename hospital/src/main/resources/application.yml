
spring:
  datasource:
    url: jdbc:postgresql://hospital-db:5432/hospital
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format_sql: true